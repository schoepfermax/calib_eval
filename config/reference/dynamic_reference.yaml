schema_version: 1

# Baseline/reference extrinsics derived from the dynamic rig URDF/xacro
# for model car 6. This is NOT ground truth.
translation: [0.0305, 0.0115, -0.0274]             # meters
rotation_quat: [0.0, 0.0, -0.3826834, 0.9238795]   # x,y,z,w (unit quaternion)

parent_frame: "camera"
child_frame: "lidar"

units:
  translation: "m"
  rotation: "quaternion_xyzw"

source:
  method: "urdf_xacro_derived"
  rig_config: "dynamic_car_6"
  dataset: "~/dataset_root/dynamic_rig/run_001"
  notes: "reference derived from car_description URDF/xacro for car 6; LiDAR yaw offset = -45 deg; baseline/reference only, not GT"

repeatability:
  # Placeholder repeatability block retained for Option C reporting.
  translation_std_m: [0.0010, 0.0012, 0.0015]   # per-axis std (m)
  rotation_std_deg: 0.35                        # scalar std (deg)